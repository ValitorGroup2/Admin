<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear center">
                            <span class="block m-t-xs headerinfo">
                                <img src="~/Content/images/VasinnFinal.png" width="70" height="70" alt="Vasinn" />
                            </span>
                            <span class="block m-t-xs headerinfo">
                                <p class="font-bold">@User.Identity.Name</p>
                            </span>
                        </span>
                    </a>
                </div>
                <div class="logo-element">
                    Vasinn
                </div>
            </li>
            @if (ViewContext.HttpContext.User.IsInRole("Admin"))
            {
                <li class="@Html.IsSelected(controller: "Home")">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-desktop"></i> <span class="nav-label">Forsíða</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Staff")">
                    <a href="@Url.Action("Index", "Staff")"><i class="fa fa-user-secret"></i> <span class="nav-label">Starfsmannayfirlit</span></a>
                    <ul class="nav nav-second-level">
                        <li class="@Html.IsSelected(action: "Index")">
                            <a href="@Url.Action("Index", "Staff")"><i class="fa fa-users"></i> Skoða starfsmenn</a>
                        </li>
                        <li class="@Html.IsSelected(action: "Create")">
                            <a href="@Url.Action("Create", "Staff")"><i class="fa fa-plus"></i> Stofna starfsmann</a>
                        </li>
                    </ul>
                </li>
                <li class="@Html.IsSelected(controller: "Transaction")">
                    <a href="@Url.Action("Index", "Transaction")"><i class="fa fa-credit-card"></i> <span class="nav-label">Söluyfirlit</span></a>
                </li>
                <li class="@Html.IsSelected(controller: "Notification")">
                    <a href="@Url.Action("Index", "Notification")"><i class="fa fa-comment"></i> <span class="nav-label">Tilkynningar</span></a>
                </li>
                <li class="@Html.IsSelected(controller: "Account")">
                    <a href="@Url.Action("Manage", "Account")"><i class="fa fa-cog"></i> <span class="nav-label">Stillingar</span></a>
                </li>
            }
            @if (ViewContext.HttpContext.User.IsInRole("ActiveUser"))
            {
                <li class="@Html.IsSelected(controller: "Home")">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-desktop"></i> <span class="nav-label">Forsíða</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Staff")">
                    <a href="@Url.Action("Index", "Staff")"><i class="fa fa-user-secret"></i> <span class="nav-label">Starfsmannayfirlit</span></a>
                    <ul class="nav nav-second-level">
                        <li class="@Html.IsSelected(action: "Index")">
                            <a href="@Url.Action("Index", "Staff")"><i class="fa fa-users"></i> Skoða starfsmenn</a>
                        </li>
                        <li class="@Html.IsSelected(action: "Create")">
                            <a href="@Url.Action("Create", "Staff")"><i class="fa fa-plus"></i> Stofna starfsmann</a>
                        </li>
                    </ul>
                </li>
                <li class="@Html.IsSelected(controller: "Transaction")">
                    <a href="@Url.Action("Index", "Transaction")"><i class="fa fa-credit-card"></i> <span class="nav-label">Söluyfirlit</span></a>
                </li>
                <li class="@Html.IsSelected(controller: "Notification")">
                    <a href="@Url.Action("Index", "Notification")"><i class="fa fa-comment"></i> <span class="nav-label">Tilkynningar</span></a>
                </li>
                <li class="@Html.IsSelected(controller: "Account")">
                    <a href="@Url.Action("Manage", "Account")"><i class="fa fa-cog"></i> <span class="nav-label">Stillingar</span></a>
                </li>
            }
            @if (ViewContext.HttpContext.User.IsInRole("InActiveUser"))
            {
                <li class="@Html.IsSelected(controller: "Home")">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-desktop"></i> <span class="nav-label">Forsíða</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Account")">
                    <a href="@Url.Action("Manage", "Account")"><i class="fa fa-cog"></i> <span class="nav-label">Stillingar</span></a>
                </li>
            }
            @if (ViewContext.HttpContext.User.IsInRole("Partner"))
            {
                <li class="@Html.IsSelected(controller: "Home")">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-desktop"></i> <span class="nav-label">Forsíða</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Partner")">
                    <a href="@Url.Action("Companies", "Partner")"><i class="fa fa-user-secret"></i> <span class="nav-label">Fyrirtækjayfirlit</span></a>
                </li>
                <li class="@Html.IsSelected(controller: "Account")">
                    <a href="@Url.Action("Manage", "Account")"><i class="fa fa-cog"></i> <span class="nav-label">Stillingar</span></a>
                </li>
            }
        </ul>
    </div>
</nav>